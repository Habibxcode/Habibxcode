name: generate animation

on:
  # run automatically every 24 hours
  schedule:
    - cron: "0 */24 * * *"
  
  # allows to manually run the Job at any time
  workflow_dispatch:
  
  # run on every push on the main branch
  push:
    branches:
    - main
    
jobs:
  generate:
    permissions: 
      contents: write
    runs-on: ubuntu-latest
    timeout-minutes: 5
    
    steps:
      # generates a snake game from a github user (<github_user_name>) contributions graph, output a svg animation at <svg_out_path>
      - name: generate github-contribution-grid-snake.svg
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: habibxcode
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          
      # push the contentTo build a GitHub profile README like the one in your image—featuring a custom terminal window with ASCII art, system information, and interactive elements—you will need to combine a few Markdown and SVG techniques.

Here is a step-by-step guide and template customized for your account (`habibxcode`).

---

### Step 1: Copy the Profile README Template

Open the `README.md` file inside your **habibxcode/habibxcode** repository and paste the following template:

```markdown
<div align="center">

```text
+-----------------------------------------------------------------------------------+
|  HABIB UR REHMAN -- ~/profile.sh -- live                                     x  - |
+-----------------------------------------------------------------------------------+
|  [ASCII ART / PORTRAIT]          SYSTEM INFO                                      |
|  ......................          ...............................................  |
|  ...  /|___/\  ........          Name........: Habib Ur Rehman                    |
|  ..  (  o.o  ) ........          Role........: CS Student / Developer             |
|  ...  > ^ <  ..........          Education...: BS CS @ ITU                        |
|  ......................          Core Lang...: C++, Python, JavaScript            |
|  ......................          Tools.......: VS Code, Git, Firebase             |
|  ......................          Status......: Building & Learning                |
+-----------------------------------------------------------------------------------+
